
<main>
    {{#if products.length}}
        <div class="grid">
            <ul>
            {{#each products}}
         <li>{{this.productData.title}} ({{this.quantity}})

            <form method="POST" action="/shop/delete-item">
            <input type="hidden" name="id" value="{{this.productData.id}}" />
                <button class="btn" type="submit">Delete</button>
            </form>
         </li>

                {{!-- <article class="card product-item">
                    <header class="card__header">
                        <h1 class="product__title">{{this.productData.title}}</h1>
                    </header>
                    <div class="card__image">
                        <img src="{{this.productData.imageUrl}}" alt="{{this.productData.title}}"
                        >
                    </div>
                    <div class="card__content">
                        <h2 class="product__price">${{this.productData.price}}</h2>
                        <p class="product__description">{{this.productData.description}}</p>
                    </div>
                    <div class="card__actions">
                        <form action="/shop/cart" method="POST">
                            <input type="hidden" name="productId" value="{{this.productData.id}}" />
                            <button class="btn" type="submit">Remove From Cart</button>
                        </form>
                    </div>
                </article> --}}

        
            {{/each}}
            </ul>
        </div>
    {{else}}
        <h1>No products found!</h1>
    {{/if}}
</main>
